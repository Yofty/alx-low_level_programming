#include <stdio.h>
/**
 * main - Entry point
 * prints the alphabet in lowercase
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char c;

	for (c = 'a' ; c <= 'z' ; c++)
	{
		putchar(c\n);
	}
	return (0);
}
