#include "main.h"
/**
 * mul - multiplies two integers
 * @a : a is the int user enter
 * @b :b is the int user inters
 *
 * Return: int
 */
int mul(int a, int b)
{
	int p;

	p = a * b;
	return (p);
}
